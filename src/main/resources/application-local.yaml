spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/mydb
    username: myuser
    password: mypassword
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # istəsən: none / validate / create / create-drop

server:
  port: 8080